# Produktions-Override: fügt nginx ins externe Traefik-Netz ein.
# Verwendung: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  nginx:
    networks:
      - proxy

networks:
  proxy:
    external: true
    name: traefik_proxy
